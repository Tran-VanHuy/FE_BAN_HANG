import { PageData } from "../../interface/base";
import { TypeFormCategoryProducts } from "./type";

export const transformDataCategoryProducts = (res: PageData<TypeFormCategoryProducts>) : PageData<TypeFormCategoryProducts> => {

   return res;
}