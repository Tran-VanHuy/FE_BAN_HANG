import { TypeFormProducts } from "./type";

export const transformDataProducts = (res: TypeFormProducts) => {
    return res
}

export const transformDataProductsDetail = (res: TypeFormProducts) => {
    
    return res
}

export const transformDataLikeProducts = (res: TypeFormProducts) => {
    return res
}
